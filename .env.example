# Copy to .env and fill in as needed.

# Backend selection lives in src/utils/backendConfig.js (USE_LOCAL_BACKEND
# toggle, production https://server.manchly.com by default — same pattern as
# the mobile app's Urls.tsx). In `npm run dev` all requests go through the
# vite proxy (/__api + /socket.io), so CORS never blocks local development.
#
# Optional override for production builds only (e.g. staging):
# VITE_API_BASE=https://server.manchly.com

# ZegoCloud 1:1 video calls (web needs AppID + ServerSecret; the mobile
# AppSign cannot be used in the browser). Without these, calls still run the
# session lifecycle (ring/accept/end/billing) but without in-browser video.
# VITE_ZEGO_APP_ID=2106372521
# VITE_ZEGO_SERVER_SECRET=your_32_char_server_secret
